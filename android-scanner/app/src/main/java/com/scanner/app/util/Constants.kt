package com.scanner.app.util

object Constants {
    const val POLL_INTERVAL_MS = 5000L
    const val RECONNECT_INTERVAL_MS = 5000L
    const val MAX_RECONNECT_ATTEMPTS = 3
    const val MAX_RECENT_RECORDS = 20
    const val SCAN_INPUT_DELAY_MS = 100L
    const val DEFAULT_DEVICE_ID = "android_scanner"
}
